name: TuskOrders

on:
  push:
    branches: [main]
    paths:
      - 'tuskorders/**'
      - 'e2e/**'
      - 'gradle/**'
      - '*.gradle.kts'
      - 'gradle.properties'
      - '.github/workflows/tuskorders.yml'
  pull_request:
    paths:
      - 'tuskorders/**'
      - 'e2e/**'
      - 'gradle/**'
      - '*.gradle.kts'
      - 'gradle.properties'
      - '.github/workflows/tuskorders.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v6
      # TuskOrders depends on Vault; the e2e scenarios use the in-memory test economy instead
      - run: ./gradlew :tuskorders:build :e2e-test-economy:build
      - uses: actions/upload-artifact@v7
        with:
          name: TuskOrders
          path: tuskorders/build/libs/TuskOrders-*.jar
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        with:
          name: TestEconomy
          path: e2e/fixtures/test-economy/build/libs/*.jar
          if-no-files-found: error

  # The jar targets the 1.21.4 API; make sure the sources still compile against the newest API.
  api-compat:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: |
            25
            21
      - uses: gradle/actions/setup-gradle@v6
      - name: Compile against Paper API 26.2
        run: ./gradlew :tuskorders:compileJava -PpaperApi=26.2.build.128-stable

  e2e:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    name: e2e ${{ matrix.scenario }} (${{ matrix.server }} ${{ matrix.mc }})
    strategy:
      fail-fast: false
      matrix:
        # Bots up to 26.1.2 (the newest protocol mineflayer speaks); console smoke test on 26.3.
        include:
          - { scenario: tuskorders, server: paper, mc: '1.21.4' }
          - { scenario: tuskorders, server: paper, mc: '1.21.11' }
          - { scenario: tuskorders, server: paper, mc: '26.1.2' }
          - { scenario: tuskorders, server: paper, mc: '26.3' }
          - { scenario: tuskorders, server: folia, mc: '1.21.11' }
          - { scenario: tuskorders, server: folia, mc: '26.1.2' }
          - { scenario: tuskorders-extra, server: paper, mc: '26.1.2' }
          - { scenario: tuskorders-extra, server: folia, mc: '1.21.11' }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: |
            25
            21
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: e2e/package-lock.json
      - uses: actions/download-artifact@v8
        with:
          name: TuskOrders
          path: tuskorders/build/libs
      - uses: actions/download-artifact@v8
        with:
          name: TestEconomy
          path: e2e/fixtures/test-economy/build/libs
      - run: npm ci
        working-directory: e2e
      - run: node run.js --scenario ${{ matrix.scenario }} --server ${{ matrix.server }} --mc ${{ matrix.mc }}
        working-directory: e2e
      - if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: server-logs-${{ matrix.scenario }}-${{ matrix.server }}-${{ matrix.mc }}
          path: e2e/.run/*/logs/latest.log
          # .run is a dot directory, which upload-artifact skips by default
          include-hidden-files: true
